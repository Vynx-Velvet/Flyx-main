# Root configuration for Cloudflare Workers Builds.
# The connected Worker name is supplied by Cloudflare through
# WRANGLER_CI_OVERRIDE_NAME; locally this defaults to flyx-landing.
name = "flyx-landing"
compatibility_date = "2026-08-07"

[assets]
directory = "./landing"
