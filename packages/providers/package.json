{
  "name": "@flyx/providers",
  "version": "3.0.2",
  "private": true,
  "description": "Flyx provider system: abstract base classes, decorator registry, and 20+ content providers",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./base": "./src/base/index.ts",
    "./registry": "./src/registry/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@flyx/core": "*",
    "@flyx/config": "*"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.1.0"
  }
}
