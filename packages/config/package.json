{
  "name": "@flyx/config",
  "version": "3.1.0",
  "private": true,
  "description": "Flyx shared configuration: provider priorities, env validation, constants",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.1.0"
  }
}
