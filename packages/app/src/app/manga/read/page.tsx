import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Manga Reader — Flyx",
};

export default function MangaReadRedirect() {
  // Redirect handled by layout / parent — this is a fallback
  return null;
}
