# npm 12+ blocks install scripts by default.
# These packages ship platform binaries and need their scripts to run.
# Setting allow-scripts=true restores the pre-npm-12 default (all scripts run).
# Safe because tls-client (and its native ffi-napi/ref-napi deps) was removed.
allow-scripts=true
